## Verbatim Requirements

- [ ] 0.1 KatanA は KDV を経由し、KRR がブラウザエンジンで評価・描画した HTML を表示する。KatanA 自身は HTML/CSS/JavaScript interpreter または browser surface を持たない。
- [x] 0.2 KRR は持続 browser page を source of truth とし、CSS layout、Web API、JavaScript event loop を browser engine に委譲する。V8 疑似 DOM、独自 CSS layout、正規表現 HTML parser、egui、KDV/KatanA WebView を採用しない。
  - Current verification: public `HtmlRuntime::open` returns the KRR-owned persistent browser session; Chromium child integration covers HTML/CSS/JavaScript, action-driven repaint, form input, timers/microtasks, CSS animation, navigation events, resource policy, and packaged Chromium resolution without `KRR_CHROME_BIN`. The local `html_browser_preview` example produces browser-rendered captures for initial render, accordion click, button DOM mutation, text input, and a link-navigation probe. `renderer_transfer` and `release_check` keep the static DOM/V8 bridge out of the interactive public API, and forbidden dependency checks keep egui, KDV, KatanA, and WebView out of KRR.
- [ ] 0.3 KRR `0.4.0`、KDV `0.3.0`、KatanA `v0.22.33` は公開済み crate だけで直列に接続し、最終 release は user review の明示 OK 後にだけ行う。
  - Development rule: 公開待ちは実装停止条件ではない。isolated worktree の一時的な Cargo patch で KRR -> KDV -> KatanA を接続して先に end-to-end 検証し、path/git override を最終 commit、lockfile、package に残さない。公開後は registry dependency と checksum 検証済み runtime asset へ置換して同じ gate を再実行する。
  - Patch rule: KatanA 統合で上流不備を検出した場合は KRR `0.4.x` / KDV `0.3.x` の隣接 patch で修正し、KatanA の release target は `v0.22.33` のまま全検証をやり直す。
- [ ] 0.4 KatanA は local `.html` / `.htm` file と、ユーザーが入力した `http/https` URL の両方を HTML browser tab として開ける。KatanA は主文書を取得し、raw HTML と完全な document URL origin を KDV 経由で KRR browser session に渡す。KDV は転送のみとし、KDV/KatanA は HTML renderer を持たない。

## 1. Runtime Boundary And Version Target

- [ ] 1.1 crates.io / GitHub Release の KRR `0.3.8` と KDV `0.2.7` を再確認し、release guard が KRR `0.4.0` と KDV `0.3.0` だけを受理することをテストする。
  - Current verification: KRR guard is corrected to require exactly latest public `v0.3.8` and target `v0.4.0`; its regression test accepts `0.4.0` and rejects `0.3.9`, `0.3.10`, `0.4.1`, `0.5.0`, `1.0.0`, and `2.0.0`. KDV guard still accepts both `v0.2.8` and `v0.3.0` after public `v0.2.7`, so this task remains incomplete until the KDV `0.3.0` phase hardens that gate.
- [x] 1.2 未公開 KRR `0.3.9` static renderer は `0.4.0` DOM runtime の foundation として扱い、static-only version を publish target にしないことを tasks と release evidence に記録する。
- [ ] 1.3 KRR が所有・配布する browser engine adapter を選定し、KRR dependency graph に egui、KDV、KatanA、KDV/KatanA WebView がないこと、browser binary/version/license を release artifact として検証する。
  - Current verification: KRR は `headless_chrome` adapter と helper child process で Chrome for Testing `150.0.7871.115` を所有する。`vendor/chromium/150.0.7871.115/manifest.json` に `BSD-3-Clause` license、mac-arm64/mac-x64/linux64/win64 の URL、SHA-256、実行パスを固定済み。`rtk cargo tree -p katana-render-runtime --edges normal --prefix none` と forbidden dependency grep で egui、KDV、KatanA、WebView/wry/tao/webkit が入っていないことを確認済み。release workflow には各 platform の release helper と pinned Chromium、manifest、license、checksum を同一 archive にする matrix gate を追加した。公開された GitHub Release で 4 archive と checksum を実在確認するまでは未完了とする。
- [x] 1.4 KRR workspace version と internal dependency を `0.4.0` に更新し、version guard regression test を追加する。

## 2. KRR Browser Runtime

- [x] 2.1 現在の custom DOM/V8 bridge/CSS normalizer を browser-equivalent renderer の release candidate から除外し、KRR `HtmlRuntime` / `HtmlRuntimeSession` を browser page session に再編する。
  - Current verification: public `HtmlRuntime::open` now starts the KRR-owned browser `HtmlBrowserSession`, and public `HtmlRuntimeSession` is an alias of that browser session. The former custom DOM/V8 session is renamed `StaticHtmlRuntime`, kept crate-private for static HTML export only, and its click-dispatch bridge is test-only. `renderer_transfer` fixes the public browser-session contract, and `release_check::html_interactive_runtime_excludes_static_dom_bridge_api` prevents `HtmlRuntimeEvent` / `HtmlNodeId` / `HtmlRuntimeDispatch` / `HtmlNavigationIntent` from re-entering the public interactive API.
- [x] 2.2 KRR 内で browser page を起動し、HTML5 parser、CSS cascade/layout/paint、JavaScript/Web API/event loop を engine に評価させる。
  - Current verification: `krr-html-chromium-engine` launches Chrome for Testing through `headless_chrome`, fulfills the complete source URL main-document request with unmodified raw HTML, and returns Chromium screenshots. It does not inject `<base>`/`script` into HTML or locate `<head>` with host string parsing. The integration regression uses a disk file with standards HTML and raw HTML without a doctype plus comment text containing `<head>`; Chromium reports `BackCompat`, proving the raw parser mode is preserved. `html_browser_engine` covers inline CSS/JavaScript pixels, local stylesheet/script/image resources, timer/microtask JavaScript, CSS animation refresh, form input, and navigation events.
- [x] 2.3 KRR public API は viewport 指定、初期 frame、frame 更新通知、入力座標・keyboard・focus・resize・scroll を受ける persistent session を提供する。
  - Current verification: `HtmlBrowserSession` exposes viewport, initial/latest frame, one-shot `take_frame_update()`, explicit `refresh_frame()`, navigation events, pointer/key/text/focus/scroll/resize, and persistent process lifecycle. `HtmlBrowserInput::Focus` and the frame-update contract are covered by unit tests and `html_browser_engine` integration tests.
- [x] 2.4 KatanA から受け取る完全な document URL origin を基準に、許可された local CSS、image、script または同一 origin の `http/https` subresource だけを解決し、変更時は session を安全に reload する。主文書の file/URL 取得は KRR が行わない。host filesystem escape、subprocess、unsupported scheme は KRR の request policy で拒否する。
  - Current verification: KRR accepts raw HTML plus full document origin. For `file/http/https`, KRR navigates to the document URL and fulfills only the main-document request with unchanged raw HTML, so local or same-origin subresources use the real document origin without a host-injected `<base>`. Integration tests cover same-origin HTTP script/CSS, blocked cross-origin redirect script, blocked cross-origin iframe, a cross-origin popup stopped before its first main request, local resource loading, local filesystem escape rejection, read-only source directories, and safe session reload through `navigate`.
- [x] 2.5 browser engine が生成した viewport と同一寸法の pixel surface を KDV に返す。KDV/KatanA は DOM、CSS、layout、hit-test を解釈しない。
  - Current verification: `HtmlBrowserFrame` validates RGBA buffer size against the requested viewport, Chromium screenshot dimensions are checked against viewport, resize integration updates the frame to the new viewport, and KDV/KatanA-side DOM/CSS/layout/hit-test implementation is still prohibited.
- [x] 2.6 JavaScript exception、navigation failure、runtime crash、execution timeout を typed error に変換し、古い frame を成功扱いにしない。
  - Current verification: child wire error codes are mapped to public `HtmlBrowserEngineErrorCode` values (`invalid_message`, `protocol_version`, `invalid_request`, `chromium`, `not_loaded`, `stdin_read`, and unknown fallback) through `HtmlBrowserError::EngineRejected`; process timeout/crash paths remain typed as `ProcessTimeout` / `ProcessCrashed`; `accept_response_errors_do_not_republish_stale_frame_updates` fixes stale-frame behavior after error responses. Integration coverage fixes protocol errors, invalid UTF-8 stdin read errors, Chromium launch failures, and missing packaged/override browser paths.
- [x] 2.7 browser engine process の lifecycle、resource limit、crash recovery を KRR が管理し、KDV/KatanA process に browser capability を漏らさない。
  - Current verification: KRR owns the browser child through `HtmlBrowserProcess` / `HtmlBrowserSession`; child processes are terminated on timeout, explicit close, and Drop. Process-level failures detach the child handle, `recover_process()` restarts the KRR-owned helper with the current raw HTML and viewport, and tests fix crash detach/recovery plus process-failure classification. Resource limits remain KRR-side through source size validation, viewport validation, request timeout, latest-frame coalescing, and request policy; KDV/KatanA only see typed errors, frames, input, and navigation events.

## 3. Interaction And Navigation Contract

- [x] 3.1 pointer、keyboard、text input、focus、scroll、resize を KDV から KRR browser session へ渡し、JavaScript action 後の frame を KDV へ返す。
  - Current verification: `html_browser_engine` forwards pointer click, keyboard, text input, surface focus, scroll, resize, and explicit frame refresh through `HtmlBrowserSession`; JavaScript action frames are returned as browser pixel frames without KDV-side DOM/hit-test semantics.
- [x] 3.2 `onclick`、`addEventListener`、form control、timer、microtask、CSS animation を browser engine の通常動作として評価し、KDV が action semantics を再実装しない。
  - Current verification: Chromium integration tests cover link/button click handling, `addEventListener`, form input listeners, timer-driven DOM updates, Promise microtasks, CSS animation frame refresh, and `preventDefault` respecting browser event semantics.
- [ ] 3.3 KatanA は URL input action から `http/https` の主文書を取得して browser tab を開く。local link と URL navigation は KRR の navigation event として KDV 経由で返す。KatanA は navigation target の次の主文書を取得して raw HTML と完全な document URL origin を再投入し、KRR は許可された subresource の URI 解決だけを担う。KDV は転送のみとし、KDV/KatanA は DOM navigation を実装しない。
- [x] 3.4 KRR request policy が host filesystem escape、subprocess、unsupported scheme を拒否し、`file` と `http/https` の allowed URL、redirect、iframe policy を integration test で固定する。
  - Current verification: `html_browser_engine` fixes local allowed resources and local outside-directory blocking; `html_browser_resource_policy` fixes same-origin HTTP resources, cross-origin redirect blocking, cross-origin iframe blocking, and browser-level popup pause/close before the cross-origin server receives a main request; policy unit tests fix same-origin HTTP, local canonical children, malformed URL, unsupported scheme, and outside-file rejection.

## 4. Regression And Release Gates

- [x] 4.1 browser engine 実測で HTML/CSS layout、external local stylesheet/script/image、DOM mutation、form input、timer/microtask、scroll、resize を KRR integration test に追加する。
  - Current verification: `rtk cargo test -p katana-render-runtime --test html_browser_engine` covers HTML/CSS layout pixels, local stylesheet/script/image resources, DOM mutation through JavaScript, form input, timer/microtask updates, scroll, resize, and browser frame refresh.
- [x] 4.2 browser engine 実測で click handler、`addEventListener`、internal navigation、`preventDefault`、CSS animation frame 更新を KRR integration test に追加する。
  - Current verification: `html_browser_engine` covers click handler and `addEventListener` dispatch, local link navigation with fragment retention, current-tab and new-tab `preventDefault`, `javascript:` link actions, same-document anchor/hash behavior retained inside Chromium, timer navigation without input, actual new-tab target navigation, and CSS animation updates via `HtmlBrowserSession::refresh_frame()`. KRR no longer injects a click/navigation script or interprets `href`; CDP Fetch confirms current-tab top-level `Document` requests after browser event dispatch. New-tab navigation uses browser-emitted `Page.windowOpen` paired with a browser-level `Target.setAutoAttach(waitForDebuggerOnStart=true, flatten=true)` page target; KRR closes that target before request/script execution and does not infer navigation from the DOM or a post-load tab list. A page script that writes the former global navigation variable cannot forge the host event.
- [x] 4.3 KRR public API / dependency graph / rustfmt / clippy / workspace tests / `just coverage` を実行し、line coverage `100% / 0 uncovered` を通す。
  - Invariant: threshold 緩和、coverage 除外、ignored test、lint/AST/release gate の無効化を禁止する。不合格は実装または契約テストを修正して解消する。
  - Current verification: `rtk cargo test -p katana-render-runtime --test html_browser_engine` passed with 25 browser integration tests and no `KRR_CHROME_BIN`; the same test with `--locked` also passed. The integration set now includes link navigation, click/action repaint, accordion-like `preventDefault` behavior, form text input, timers/microtasks, CSS animation, resize/scroll, packaged Chromium resolution, and the `KRR_CHROMIUM_NO_SANDBOX=1` child-process override without disabling rendering. `rtk cargo test -p katana-render-runtime --test html_browser_resource_policy --locked` passed. `rtk just check` passed; rustfmt, strict clippy, workspace tests, AST lint, dependency leak check, runtime asset checks, Chromium manifest/install helper checks, Chromium helper script tests, package-file checks, biome, and typecheck are included in that gate. `rtk just coverage` passed strict `--fail-under-lines 100 --fail-uncovered-lines 0`; `rtk cargo llvm-cov report --summary-only --fail-under-lines 100 --fail-uncovered-lines 0` confirmed `TOTAL ... Lines 9041, Missed Lines 0, Cover 100.00%`, `chromium_args.rs` is `Lines 119, Missed Lines 0, Cover 100.00%`, and `screenshot.rs` is `Lines 206, Missed Lines 0, Cover 100.00%`. CI/preflight hangs were traced to repeated action-frame `Page.captureScreenshot` calls after a successful initial frame. KRR now captures a single browser screencast PNG frame, acknowledges and stops the screencast, validates/crops the returned Chromium surface to the requested viewport, initializes tab bounds before applying device metrics, and waits for stylesheet/image readiness before frame capture; Linux Chromium starts with SwiftShader through ANGLE (`--use-gl=angle`, `--use-angle=swiftshader`, `--enable-unsafe-swiftshader`), while macOS/Windows keep direct SwiftShader (`--use-gl=swiftshader`, `--enable-unsafe-swiftshader`) to avoid macOS screencast-stop hangs without forcing `--disable-gpu`.
  - Follow-up verification: GitHub preflight run `29470961769` and CI run `29470961781` failed before this update: Ubuntu missed the local script-applied initial paint in `chromium_child_resolves_local_css_javascript_and_image_from_source_origin`, and Windows kept `LOCALAPPDATA` available in the PlantUML default-cache fallback test. Follow-up preflight runs `29475202205` and `29476385194` still failed the same local script-applied initial paint after the intermediate DOMContentLoaded wait. KRR now separates initial-load readiness from frame-capture readiness: initial load waits for `document.readyState === "complete"`, stylesheet/image readiness, and a compositor paint barrier, while screenshot/input refresh uses only the lightweight double-rAF paint barrier immediately before screencast capture. GitHub preflight run `29478332444` confirmed the HTML browser integration itself passed 25/25, then failed PlantUML tests because the release-preflight workflow did not install Graphviz before `release-check`; the workflow now installs Graphviz before release-check and `release_preflight_installs_graphviz_before_release_check` fixes that gate contract. Follow-up preflight run `29479793757` still reproduced the local script-applied initial-paint miss on Ubuntu, proving the remaining race was before loaded-rendering sync. KRR now retries loaded-rendering sync until `location.href` matches the expected document URL, so readiness is evaluated against the committed document instead of a previous page. CI run `29479793753` passed macOS and Ubuntu but failed Windows because `release_check.rs` executed `check-openspec-release-archive.sh` directly; the integration test now invokes the shell script through `bash` for Windows CI. The PlantUML public fallback test also unsets platform cache env (`LOCALAPPDATA` / `XDG_CACHE_HOME`) when asserting temp fallback. Follow-up preflight run `29510393436` still reproduced the local script-applied initial-paint miss, so loaded sync now waits for external script `load` / `error` / `readystatechange` completion before image readiness and the paint barrier. CI run `29510393510` passed macOS and Ubuntu tests/coverage, then failed Ubuntu only in rust-cache post-save with disk full and failed Windows in the OpenSpec archive test because Git Bash received a Windows-style script path. CI run `29513731320` still failed Windows because `bash` resolved to WSL, and Ubuntu exhausted runner disk after coverage. Preflight run `29513733063` passed the browser integration but failed strict coverage because PlantUML bridge tests were macOS-only, leaving `plantuml_renderer/runtime/bridge.rs` with 4 uncovered lines on Linux. KRR now runs the PlantUML bridge tests on every OS through the normal JVM resolver, shares the PlantUML env lock, and removes JVM override env while resolving the real runtime. The release archive test now chooses Git Bash from `GIT_BASH` / Program Files before falling back to `bash`, slash-normalizes the script path, and prints stdout/stderr on failure. GitHub preflight run `29516660926` passed, and CI run `29516661071` passed macOS and Windows, then failed Ubuntu during coverage with the Actions log blob unavailable; KRR now disables Rust target cache restore/save on Ubuntu, frees `cargo llvm-cov` / target build artifacts before and after Ubuntu coverage, and keeps CI coverage strict by forbidding `continue-on-error: true`. GitHub preflight run `29519165552` then passed the 25 HTML browser integration tests but failed the follow-up resource policy browser process on Linux GL initialization (`gl=none,angle=none` not allowed); KRR now uses ANGLE SwiftShader launch flags on Linux instead of the removed Linux `--use-gl=swiftshader` path. A macOS push-hook repro showed applying ANGLE globally can hang at `Page.stopScreencast`, so macOS/Windows retain direct SwiftShader. GitHub CI run `29523177871` passed macOS, Ubuntu, and Windows, but GitHub preflight run `29523177952` failed strict coverage when the follow-up Linux resource-policy Chromium process did not expose a DevTools endpoint within the previous 10 second startup window. KRR now uses a bounded 30 second Chromium startup timeout, keeps the startup timeout regression fixture above that bound, and avoids macOS `StopScreencast` RPCs after a captured frame because repeated macOS screencast stop calls can hang even with direct SwiftShader. The loaded-rendering script remains split into `rendering_sync/scripts.rs` to keep AST file-length gates green. Local revalidation after the startup/screencast fix: `rtk cargo test -p katana-render-runtime html_chromium_engine::chromium_process::tests::browser_connection_timeouts_cover_slow_ci_chromium --lib --locked`, `rtk cargo test -p katana-render-runtime --test html_browser_resource_policy --locked`, `rtk cargo test -p katana-render-runtime --test html_browser_engine chromium_child_applies_surface_focus_before_text_input --locked -- --test-threads=1`, `rtk cargo test -p katana-render-runtime --test html_browser_engine --locked -- --test-threads=1`, and `VERSION=v0.4.0 TEST_THREADS=1 rtk just release-check` passed. The latest full local release gate confirmed `cargo test: 646 passed (21 suites, 219.60s)` and strict `cargo llvm-cov --fail-under-lines 100 --fail-uncovered-lines 0` passed. GitHub PR #31 now has successful macOS, Ubuntu, Windows, and preflight checks on commit `02e0d8c`; KRR `0.4.0` is release-gate green but remains unpublished.
  - Desktop viewport follow-up: the preview harness exposed a real 960x720 failure where new-headless Chromium returned a 960x577 inner screencast surface. KRR now measures the actual surface deficit, expands only the browser window bounds by that measured amount, reapplies device metrics, and captures again; it does not use a platform-specific or fixed padding. The integration regression fixes exact 960x720 initial output and 1280x800 resize output. The browser preview regenerated initial, accordion-click, button-DOM-mutation, and text-input PNGs plus a link navigation event. Latest `VERSION=v0.4.0 TEST_THREADS=1 rtk just release-check` passed with 652 tests across 21 suites, Chromium integration 27/27, strict line coverage `9111 / 0 missed / 100%`, runtime archive verification, package verification, crate-size verification, and publish dry-run.
  - Raw HTML evaluation follow-up: KRR no longer writes or rewrites the interactive document through a host temporary file. Chromium navigates to the complete original `file` or `http/https` document URL, KRR fulfills only the first top-level main-document request with the caller's raw HTML unchanged, and relative resources resolve against the browser-visible document origin. Navigation monitoring uses confirmed CDP requests/targets and does not inject a `<base>` or `<script>` element. A regression with a fragment-bearing file origin, no doctype, and a comment containing `<head>` proves Chromium preserves `BackCompat` instead of accepting a host-rewritten standards document. Final local `VERSION=v0.4.0 TEST_THREADS=1 rtk just release-check` passed with 647 tests across 21 suites, Chromium integration 33/33, strict line coverage `9004 / 0 missed / 100%`, runtime archive verification, package verification, crate-size verification, and publish dry-run.
  - Full-surface follow-up: `chromium_screencast_action_frame_preserves_unchanged_page_content` runs the packaged-preview sequence at `960x720` and requires the initial, accordion, button, and text-input generations to retain the exact heading pixel at `(80,80)` while exposing each action result. The targeted regression passed three consecutive runs; after self-review strengthened the assertion from color presence to the exact coordinate, the targeted test, AST lint, and test-target clippy passed again. `rtk just coverage` passed Chromium integration `34/34`, resource policy `2/2`, AST lint, and strict line coverage `9304 / 0 missed / 100.00%` without exclusions or threshold changes.

- [x] 4.4 `just VERSION=0.4.0 release-verify`、package 内容、publish dry-run、OpenSpec strict validation を通す。公開開始前に user approval を確認する。
  - Current verification: `rtk just release-verify` passed for version `0.4.0`; `verify-release-target.py`, tag safety, unpublished crates.io target checks, Chromium archive install/check, internal dependency version verification, package checks, crate-size guard (`target/package/katana-render-runtime-0.4.0.crate size 10094613/10485760 bytes`), and `cargo publish --dry-run` all passed. Only dry-run publication was executed. OpenSpec strict validation and local `VERSION=v0.4.0 TEST_THREADS=1 rtk just release-check` passed. GitHub PR #31 checks cover the earlier commit, while the current root HTML/navigation and release-asset updates remain local.
  - Guard follow-up: after correcting the release target gate to accept only `v0.4.0` after public `v0.3.8`, `rtk just VERSION=v0.4.0 TEST_THREADS=1 release-check` passed again. The gate reported 646 tests across 21 suites, strict line coverage `100% / 0 uncovered`, Chromium manifest/install checks, package verification, crate-size verification, and `cargo publish --dry-run`; the upload was aborted by dry-run. GitHub checks cover commit `02e0d8c`; the follow-up guard and evidence updates are included in the current release branch update.
  - Final local verification: `rtk env VERSION=v0.4.0 just release-verify` passed after the full-surface regression was added. The package contains 424 files, the crate-size guard reported `10099440/10485760 bytes`, and `cargo publish --dry-run` reached upload then aborted as required. No commit, push, PR mutation, publish, or release was performed.
  - Final monolithic gate: `rtk env VERSION=v0.4.0 TEST_THREADS=1 just release-check` passed with 662 workspace tests across 21 suites, Chromium integration `34/34`, resource policy `2/2`, AST/Biome/TypeScript/runtime digest checks, strict line coverage `9304 / 0 missed / 100.00%`, four-platform runtime archive contracts, package verification, crate-size verification, and publish dry-run. The dry-run aborted the upload; no external mutation was performed.
  - 2026-07-18 user instruction: the earlier publication prohibition is not active. Proceed through the existing `release/v0.4.0` PR and repository release workflow while starting KDV `0.3.0` and KatanA integration through isolated local verification; replace temporary patches with registry dependencies and verify published assets before downstream release.
- [ ] 4.5 KRR release workflow は linux64、mac-arm64、mac-x64、win64 の各 runner で release helper を build し、manifest の Chromium archive を download・SHA-256 検証・helper 隣接へ展開して、再現可能な runtime archive と checksum を生成する。全 archive を GitHub Release へ crates.io publish より先に immutable upload し、既存同名 asset が異なる場合は release を拒否する。KatanA release packaging は公開済み asset の checksum を検証して executable 隣接へ配置する。
  - Current implementation: `package_runtime.py` は helper、`chromium/<platform>`、source manifest、KRR license、runtime manifest を一体化し、archive member、実行 bit、Chromium checksum marker、helper/license/source-manifest digest を自己検証する。synthetic contract test は 4 platform、tar/zip、timestamp-independent checksum、tampered marker、packaged-file digest mismatch、cross-platform runner mismatch を固定する。CI と release workflow は同じ 4 runner matrix を持ち、merge 前にも各 platform の optimized helper と実 Chromium bundle を archive 化する。release packaging は pinned source ZIP を再検証して fresh directory へ展開するため、cache 内の tampered executable または stale file を再利用しない。release workflow の全 job は merged PR の immutable commit SHA を checkout し、4 archive/checksum を draft GitHub Release へ non-clobber uploadしてから Release を公開し、その後にだけ crates.io publish を実行する。upload script test は初回8 asset、既存同一 asset、既存差分拒否、欠損、local checksum mismatch を GitHub CLI test double で固定する。実 Release asset と downstream KatanA artifact の確認前なので未完了。
  - Local artifact evidence: optimized mac-arm64 helper と実 Chrome for Testing bundle から fresh install で `186552500` byte の archive を生成し、SHA-256 `f3dc17698fedae6a75710d5f0b80823d6bc08ffd00f49d821dcfbaae2490a483` を検証した。archive を新規 directory へ展開し、同梱 helper を `KRR_CHROME_BIN` なしで起動した preview smoke は initial frame、accordion click、button DOM mutation、text input、link navigation を完了した。4 capture はすべて `960x720` で、不変の heading pixel と各 action state を保持し、文字の重なりがない。これは local mac-arm64 artifact の証跡であり、4 runner の公開 asset 証跡には代用しない。

## 5. KDV And KatanA Handoff

- [ ] 5.1 KRR `0.4.0` が crates.io 公開済みであることを確認した後にだけ、KDV を crates.io `^0.4.0` に更新する。
  - Current verification: live `rtk cargo info katana-render-runtime@0.4.0 --registry crates-io` returned not found, `rtk gh release view v0.4.0 --repo HiroyukiFuruno/katana-render-runtime` returned release not found, and GitHub Release `v0.3.8` remains latest. KDV release manifest cannot switch to registry KRR `^0.4.0` until publication, but adapter implementation and KatanA end-to-end verification may proceed through an isolated, uncommitted local patch. Existing KDV worktrees contain uncommitted `0.2.8` static HTML parser/CSS/visibility work and one local KRR patch; these are preserved as pre-existing work but are not accepted as the KDV `0.3.0` browser-session adapter.
- [ ] 5.2 KDV の HTML→PDF/画像 export 実装は維持する。KatanA interactive viewer 用の direct HTML parser / CSS cascade / visibility / table normalizer と image control だけを、KRR browser frame surface、入力中継、frame 更新、navigation event の consumer に置換して KDV `0.3.0` の release gate を通す。
  - Pre-publication audit: KRR already exposes `HtmlRuntime::open_packaged`, `HtmlBrowserSession`, RGBA `HtmlBrowserFrame` generations, pointer/key/text/focus/scroll/resize input, navigation events, reload, recovery, and close. KDV `0.2.7` routes HTML through `DirectHtmlNormalizer` into `PreviewOutput` / `PreviewSurfaceImage`; this is a static parser/export-style path and must not become the interactive adapter. The current KatanA v0.22.33 worktree converts that surface into `RenderedSection::Image`, then paints it with `Sense::hover()`, so browser hit-test, form input, repaint generations, and navigation cannot pass through. After KRR publication, KDV must expose a distinct persistent browser-session adapter, and KatanA must paint its latest frame while forwarding raw UI coordinates/input and returning navigation targets to the existing main-document fetch path. Existing KatanA URL-source work already preserves raw HTML and the final full document URL, which matches the KRR source contract.
  - Adapter contract: KDV owns each synchronous KRR `HtmlBrowserSession` on a dedicated worker thread and exposes a non-blocking handle to its consumer. Typed channels preserve key/text/focus/resize/navigation ordering and errors; pointer move and repaint requests may be coalesced, and frames are latest-generation only. Compile-time KRR transfer tests require the session/input/navigation/error types to be `Send` and frame/config values to be `Send + Sync`; KDV still must not interpret HTML or browser events.
  - User feedback evidence: the current KatanA static path renders `title`, stylesheet source, and script source as struck-through body text and does not evaluate CSS/JavaScript. This proves the interactive path is not HTML evaluation. The KDV adapter must not retain a static-parser or export-image fallback; KRR startup failure must remain a typed viewer error.
- [ ] 5.3 KDV `0.3.0` が crates.io 公開済みであることを確認した後にだけ、KatanA が Cargo で KDV を解決する。KatanA build/release workflow は同じ KRR version/platform の公開済み runtime archive と checksum を取得し、helper と `chromium` directory を KatanA executable 隣接へ配置する。開発機の Chrome、Cargo build directory、local path dependency を配布成果物の runtime として使わない。
  - Packaging audit: current KatanA macOS `.app`、Linux tar、Windows ZIP は KatanA executable だけを package し、Windows WiX MSI の `binary0` component も `KatanA.exe` だけを収録するため、現状の配布物では browser session を起動できない。KRR/KDV 公開後、macOS は `Contents/MacOS` へ runtime root 内容を配置してから最終 codesign、Linux/Windows ZIP は executable/helper/`chromium` を同一 root に配置し、Windows MSI は runtime tree の WiX component group を収録する。各 asset contract は archive/MSI 内容と packaged-helper smoke を検証する。
- [ ] 5.4 KatanA native window で browser-equivalent CSS layout、JavaScript action、input、internal navigation、reload を確認する。証跡取得は browser runtime が動作し、ユーザーが再開を指示した後にだけ行う。
- [ ] 5.5 user review に実機操作可能な確認材料、browser capability policy、KRR → KDV → KatanA responsibility boundary を提示し、明示 OK まで KatanA `v0.22.33` を release しない。

## User Review Phase

- [ ] KatanA の最終実機証跡は `/Users/hiroyuki_furuno/works/private/katana-verify-html-origin-master` (`release/v0.22.33`) から取得する。旧 `v0-22-33-html-file-preview.json` の static preview は browser-equivalent viewer の証跡に使わない。最終 capture では表 header / row、本文、control の文字が背景や隣接要素へ重ならず、リンク、accordion、button action、text input、navigation の操作前後を判別できることを確認する。
  - User feedback evidence: 旧 static preview は `Feature` / `Status` header が先頭 row 背景へ食い込み、`Direct HTML preview` と視覚的に重なっている。この画面は不合格であり、旧 renderer の補修ではなく公開済み KDV browser-session adapter を使う最終 KatanA surface への置換で解消する。
